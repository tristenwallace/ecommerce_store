{
    "dev": {
        "driver": "pg",
        "user": "${POSTGRES_USER}",
        "password": "${POSTGRES_PASSWORD}",
        "host": "${POSTGRES_HOST}",
        "database": "${POSTGRES_NAME}",
        "port": "${POSTGRES_PORT}",
        "schema": "public"
    },
    "test": {
        "driver": "pg"
    },
    "production": {
        "driver": "pg"
    }
}